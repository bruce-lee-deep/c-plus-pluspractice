# 刷题目录说明

- `当前题目/main.cpp`：当前正在写、也是项目里唯一参与编译的文件。
- `题库/题目名/main.cpp`：已经保存下来的题解，不会被当前项目一起编译。
- `笔记/knowledge.md`：做题时积累的笔记。

# 使用方式

- 平时直接修改 `当前题目/main.cpp` 并运行。
- 一道题做完后，把代码复制到 `题库/你的题目名/main.cpp` 里保存。
- 然后再把 `当前题目/main.cpp` 改成下一题的代码即可。
